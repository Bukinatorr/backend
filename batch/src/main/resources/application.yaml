spring:
  batch:
    job:
      enabled: true
    jdbc:
      initialize-schema: always
      table-prefix: BATCH_
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://bukinator-db-dev.cbmm48cq2pkp.ap-northeast-2.rds.amazonaws.com/bukinator?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: qwer1234!
  jpa:
    show-sql: false
    open-in-view: false
    hibernate:
      ddl-auto: update
